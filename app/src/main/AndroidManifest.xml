<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.omar.energy4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SettingUp2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".ui.HomeActivity" />! -->


        <!-- <activity android:name=".LoginActivity" />! -->
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.UsersListActivity" />
        <activity
            android:name=".ui.RoomDetailsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.SettingUp1Activity" />
        <!-- <activity android:name="com.example.omar.energy4.ui.SettingUp2Activity"></activity>! -->
        <activity android:name=".ui.RoomSettingActivity"></activity>
    </application>

</manifest>